multiplier: ${globals:multiplier}

# llms: ['OpenAILLM', 'FalconLLM']
# embeddings: ['OpenAIEmbeddings', 'FalconEmbeddings']

models:
  falcon:
    user_local_path: /home/hfwittmann/.cache/gpt4all/ggml-model-gpt4all-falcon-q4_0.bin
  
embeddings:
  path: data/step03_store/${embedding}
  collections: ['OpenAIEmbeddings', 'FalconEmbeddings']

llms:
  evaluator: OpenAILLM
  collections: ['OpenAILLM', 'FalconLLM']

embedding: ${embedding}
llm: ${llm}
